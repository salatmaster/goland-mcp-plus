name: Build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      # The IDE runs headless here; the build already sets java.awt.headless.
      - name: Test
        run: ./gradlew test verifyPluginStructure --no-daemon

      - name: Build the plugin
        run: ./gradlew buildPlugin --no-daemon

      # Not a release artifact: this carries the gradle.properties fallback version,
      # since only a tag names a real one.
      - name: Upload the development build
        uses: actions/upload-artifact@v4
        with:
          name: plugin-dev
          path: build/distributions/*.zip
          if-no-files-found: error

      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: build/reports/tests/test
